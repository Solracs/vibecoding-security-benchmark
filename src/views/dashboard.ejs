<%- include("header") %>

<h2>Dashboard</h2>

<div class="card mt-3">
    <div class="card-body">
        <p>Welcome, <strong><%= username %></strong>!</p>
        <p>Current model in use: <strong><%= currentModel %></strong></p>
    </div>
</div>
